# Wrapper Placeholder

This directory is reserved for Wrapper runtime files.

Not committed by design:
- `wrapper` binary
- `rootfs/` runtime tree
- session/cache artifacts

Use the app's setup flow (or existing local setup scripts) to install Wrapper locally.
